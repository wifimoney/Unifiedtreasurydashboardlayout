
  # Unified Treasury Dashboard Layout

  This is a code bundle for Unified Treasury Dashboard Layout. The original project is available at https://www.figma.com/design/SRBQZUrPfQ4yYzQfUbBaIH/Unified-Treasury-Dashboard-Layout.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  